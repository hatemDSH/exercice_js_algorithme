ALGORITHM somme_2_nombres
VAR
    X , Y : INTEGER
    sum : INTEGER := 0 
BEGIN
    
     REPEAT
    
       write(" donner un entier  X= ")
       Read(X)
       write(" donner un entier  X = ")
       Read(Y)

     sum = X + Y 

     UNTIL (  sum > 20)
       
       write("la somme de X= ",X   " +  Y = ", Y , "est " , sum )

END